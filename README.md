This repository contains Autogen Tool example to illustrate how it can be designed.

src.template, header.template and input.xls are taken as input to generate the output folder with the script written.
